const max = (array) => Math.max(...array);
const min = (array) => Math.min(...array);
// max([]); // returns the max value of given values (array)
// min([]); // returns the min value of given values (array)

export { max as max, min as min };
